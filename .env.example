# =============================================================================
# NEUSEARCH PRODUCT ASSISTANT - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and customize values for local development
# 
# IMPORTANT: No API keys are required for the demo to work!
# The app runs in fallback mode without OpenAI.
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION (for local docker-compose)
# -----------------------------------------------------------------------------
POSTGRES_DB=neusearch_db
POSTGRES_USER=neusearch
POSTGRES_PASSWORD=neusearch_dev_password_12345

# For backend connection (automatically set in docker-compose)
# DATABASE_URL=postgresql://neusearch:neusearch_dev_password_12345@db:5432/neusearch_db

# -----------------------------------------------------------------------------
# OPENAI API (OPTIONAL - App works without this!)
# -----------------------------------------------------------------------------
# Leave blank to use fallback keyword matching mode (no API calls)
# Add your key here if you want LLM-powered recommendations
OPENAI_API_KEY=

# -----------------------------------------------------------------------------
# FRONTEND CONFIGURATION
# -----------------------------------------------------------------------------
# Backend API URL (for local dev, use localhost; for production, use deployed URL)
REACT_APP_API_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# DEPLOYMENT NOTES
# -----------------------------------------------------------------------------
# For Render deployment:
#   - DATABASE_URL is automatically provided by Render's managed Postgres
#   - Set OPENAI_API_KEY in Render dashboard (optional)
#   - No .env file needed on Render
#
# For Vercel deployment (frontend):
#   - Set REACT_APP_API_URL to your Render backend URL
#   - Example: REACT_APP_API_URL=https://neusearch-backend.onrender.com
#
# For local Docker development:
#   - Copy this file to .env
#   - Run: docker-compose up --build
#   - Frontend: http://localhost:3000
#   - Backend: http://localhost:8000